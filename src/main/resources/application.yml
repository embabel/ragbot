ragbot:
  chunker-config:
    max-chunk-size: 800
    overlap-size: 100

  chat-llm:
    model: gpt-4.1-mini
    temperature: 0.0

  persona: sir-humphrey
  objective: legal

embabel:
  agent:
    shell:
      # If true, redirects logging to a file rather than console during chat
      redirect-log-to-file: true

